sudo: required
services:
    - docker

script:
    - docker build -t adistiffler/travis .
    - docker images adistiffler/travis

before_deploy:
    - docker login -u adistiffler -p Babu@1987

deploy:
    provider: script
    script: docker push adistiffler/travis
    on:
        branch: master
